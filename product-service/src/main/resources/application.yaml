spring:
  application:
    name: product-service
  datasource:
    url: jdbc:postgresql://192.168.56.1:5433/product-service
#    ${DATASOURCE_HOST:localhost}
    username: postgres
    password: postgres
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka
#  instance:
#    instance-id: ${spring.application.name}:${random.uuid}
server:
  port: 8081
